// flags, mines, mines_active
import React from "react";

const Entity = ({ imgsrc, key, handler }) => {
  return <img width={"200px"} height={"200px"} src={imgsrc} alt="" />;
};

export default Entity;
